name: HCHI Automated Deployment

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Trigger HCHI Sync Engine
        run: |
          sync-app hchi-dashboard